# ─────────────────────────────────────────────────────────────────────────────
# Platform observability defaults — shipped by space-quarkus-common-observability.
#
# These live in META-INF/microprofile-config.properties (config ordinal 100) so a
# service's own application.properties (ordinal 250) always wins. Adding the
# space-quarkus-common-observability (or -all) dependency is enough to inherit the
# platform standard; no per-service copy/paste required. See /monitoring/README.md.
# ─────────────────────────────────────────────────────────────────────────────

# ── Metrics (Prometheus) — INF-3 ──────────────────────────────────────────────
quarkus.micrometer.export.prometheus.enabled=true
quarkus.micrometer.export.prometheus.path=/q/metrics
quarkus.micrometer.binder.http-server.enabled=true

# ── Tracing (OpenTelemetry) — INF-4 ───────────────────────────────────────────
quarkus.otel.exporter.otlp.traces.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.otel.traces.sampler=parentbased_traceidratio
quarkus.otel.traces.sampler.arg=${OTEL_TRACES_SAMPLER_ARG:1.0}

# Keep `mvn verify` Docker-free: SDK disabled under the test profile.
%test.quarkus.otel.sdk.disabled=true
